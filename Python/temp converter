#now this took me a solid minute, I made it so it'll ask for the temperature and whether it is Fahrenheit or Celsius
#once you have answered both prompts itll go even further to convert either or to the opposite 
temp = input('Temperature: ')
temp2 = int(temp)
Deg = input("Fahrenheit or Celsius: ")

if Deg.upper() == "F" or "Fahrenheit":
    Celsius = int(round((temp2 - 32) * 5 / 9))
    print("temp in celsius converted: " + str(Celsius))
else:
        Fahrenheit = int(round((9 * temp2) / 5 + 32))
        print ('temperature in fahrenheit: ' + str(Fahrenheit))
